enum ViewStateGet { Loading, hasData, Empty, Error }

enum ViewStateCreate { Creating, Success, Error }

enum AuthAccessStatus { Checking, Teacher, Student, Empty }

enum ConnectionStatus { Wifi, Mobile, Offline }
