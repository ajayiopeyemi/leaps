enum ResourceType { Text, Pdf, Image, Video }

enum TransitionType { Slide, Scale, Fade }

enum UserType { teacher, student }

enum NavigationType { Push, PushReplacement, PopUntilFirst, Pop}

enum MessageType {Success, Error, Warning}

enum RegistrationUseCase{Country, School, Class}